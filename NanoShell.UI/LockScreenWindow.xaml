<Window x:Class="NanoShell.UI.LockScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual"
        Background="Black"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"
        Height="{x:Static SystemParameters.PrimaryScreenHeight}"
        Left="0" Top="0"
        Focusable="False"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Loaded="Window_Loaded">
    <Grid x:Name="RootGrid">
        <Rectangle x:Name="SolidBg" Fill="Black" Visibility="Visible"/>
        <Image x:Name="WallpaperImage" Stretch="UniformToFill" Visibility="Collapsed"/>
        <Border x:Name="GradientOverlay" Background="#80000000" Visibility="Collapsed"/>
        <TextBlock x:Name="TimeText"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="72"
                   FontWeight="Light"
                   Visibility="Collapsed"/>
        <TextBlock x:Name="DateText"
                   Foreground="#CCC"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="24"
                   Margin="0,80,0,0"
                   FontWeight="Light"
                   Visibility="Collapsed"/>
        <TextBlock x:Name="AODTimeText"
                   Foreground="#555"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="36"
                   FontWeight="Light"
                   Visibility="Collapsed"/>

        <!-- Gear button -->
        <Button x:Name="GearButton" Content="&#x2699;" Foreground="#666"
                BorderThickness="0" FontSize="28" Cursor="Hand" Width="48" Height="48"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,16,16,0" Visibility="Collapsed"
                FocusVisualStyle="{x:Null}"
                ToolTip="Suspend settings"
                Click="GearButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="#01000000">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#22AAAAAA"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Process panel overlay -->
        <Grid x:Name="PanelOverlay" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Rectangle Grid.Column="0" Fill="Transparent" MouseDown="Overlay_MouseDown"/>
            <ContentControl Grid.Column="1" x:Name="PanelHost"/>
        </Grid>
    </Grid>
</Window>
