<UserControl x:Class="NanoShell.UI.ProcessLockPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="400">
    <UserControl.Resources>
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border CornerRadius="10" Background="#555" BorderThickness="0">
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="CheckBox.Checked">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="#4CAF50" Duration="0:0:0.15"
                                                Storyboard.TargetProperty="Background.Color"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="CheckBox.Unchecked">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="#555" Duration="0:0:0.15"
                                                Storyboard.TargetProperty="Background.Color"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                            <Ellipse Width="16" Height="16" Fill="White"
                                     HorizontalAlignment="Left" Margin="2,0,0,0">
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Ellipse.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard x:Name="thumbAnim">
                                                <ThicknessAnimation x:Name="thumbMargin"
                                                    Storyboard.TargetProperty="Margin"
                                                    Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#CC151515" CornerRadius="12,0,0,12">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Processes" Foreground="White" FontSize="18" FontWeight="Light"
                           VerticalAlignment="Center"/>
                <Button x:Name="CloseButton" Content="✕" Foreground="#999" Background="Transparent"
                        BorderThickness="0" FontSize="16" Cursor="Hand"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Search -->
            <TextBox x:Name="SearchBox" Grid.Row="1"
                     Foreground="White" Background="#333" BorderThickness="0"
                     Padding="8,6" Margin="0,0,0,8"
                     TextChanged="SearchBox_TextChanged"/>

            <!-- Filter pills -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                <Border x:Name="FilterAllBg" Background="#666" CornerRadius="4" Margin="0,0,6,0">
                    <Button x:Name="FilterAll" Content="All" Foreground="White"
                            Background="Transparent" BorderThickness="0" Padding="10,4"
                            Cursor="Hand" FontSize="12" Click="FilterAll_Click"/>
                </Border>
                <Border x:Name="FilterExceptionsBg" Background="#444" CornerRadius="4">
                    <Button x:Name="FilterExceptions" Content="Exceptions" Foreground="White"
                            Background="Transparent" BorderThickness="0" Padding="10,4"
                            Cursor="Hand" FontSize="12" Click="FilterExceptions_Click"/>
                </Border>
            </StackPanel>

            <!-- Process list -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Hidden" PanningMode="Both">
                <StackPanel>
                    <TextBlock x:Name="RegularHeader" Text="REGULAR" Foreground="#888"
                               FontSize="11" Margin="0,4,0,4" FontWeight="SemiBold"
                               Visibility="Visible"/>
                    <ItemsControl x:Name="RegularList" Visibility="Visible">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="Transparent" Padding="4,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding FreezeIcon}"
                                                   Foreground="{Binding FreezeColor}" FontSize="14"
                                                   VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" Foreground="White"
                                                       FontSize="13" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding WindowTitle}" Foreground="#888"
                                                       FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        <CheckBox Grid.Column="2"
                                                  Style="{StaticResource ToggleSwitchStyle}"
                                                  IsChecked="{Binding IsException, Mode=TwoWay}"
                                                  Checked="Toggle_Checked"
                                                  Unchecked="Toggle_Unchecked"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Height="1" Background="#333" Margin="0,8"/>

                    <TextBlock x:Name="BackgroundHeader" Text="BACKGROUND" Foreground="#888"
                               FontSize="11" Margin="0,4,0,4" FontWeight="SemiBold"
                               Visibility="Visible"/>
                    <ItemsControl x:Name="BackgroundList" Visibility="Visible">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="Transparent" Padding="4,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding FreezeIcon}"
                                                   Foreground="{Binding FreezeColor}" FontSize="14"
                                                   VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                                   Foreground="White" FontSize="13"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                        <CheckBox Grid.Column="2"
                                                  Style="{StaticResource ToggleSwitchStyle}"
                                                  IsChecked="{Binding IsException, Mode=TwoWay}"
                                                  Checked="Toggle_Checked"
                                                  Unchecked="Toggle_Unchecked"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
